export const metadata = {
    title: "Admin - Games Platform NextGen",
    description: "Admin Panel - Games Platform NextGen",
};

export default function MainLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}